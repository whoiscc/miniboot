; 410_cat.misc
; segment 0, page 0x410###


0x401000

; calling convention (callee side)
; step 0, expect arguments at stack base address
; in this case nothing

; start working
; finish working

; step 1, save return value at stack base address
; dst = $D + 0
ldd
stb
shl     40
imm     7
st64

; step 2, load return address
; src = $D - 8
ldd
stb
shl     40
imm     8
neg
add
stb
ld

; step 3, jump to caller
stb
jmp
